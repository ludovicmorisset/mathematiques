services:
  multiplication:
    build: .
    container_name: multiplication-app
    networks:
      - default
    restart: unless-stopped
    ports:
      - 1987:1987
      
networks:
  default:
    external: true
    name: web
