services:
  web:
    build: .
    container_name: maths-app
    ports:
      - "1987:80"
    restart: unless-stopped
    volumes:
      - ./img:/usr/share/nginx/html/img
    networks:
      - default

networks:
  default:
    external: true
    name: web
