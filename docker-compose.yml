version: '3.8'

services:
  web:
    build: .
    container_name: maths-app
    ports:
      - "1987:80"
    volumes:
      - ./img:/usr/share/nginx/html/img
    restart: unless-stopped

networks:
  nginx-proxy:
    external: true 